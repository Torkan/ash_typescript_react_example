// Generated by AshTypescript
// Do not edit this file manually



type AshDate = string;
type Decimal = string;
type UUID = string;

// Company Schema
export type CompanyResourceSchema = {
  __type: "Resource";
  __primitiveFields: "id" | "userId" | "name" | "addressLine1" | "addressLine2" | "city" | "postalCode" | "country" | "vatNumber" | "email" | "phone" | "isDefault";
  id: UUID;
  userId: UUID;
  name: string;
  addressLine1: string;
  addressLine2: string | null;
  city: string;
  postalCode: string;
  country: string;
  vatNumber: string | null;
  email: string | null;
  phone: string | null;
  isDefault: boolean;
};



// Customer Schema
export type CustomerResourceSchema = {
  __type: "Resource";
  __primitiveFields: "id" | "userId" | "name" | "addressLine1" | "addressLine2" | "city" | "postalCode" | "country" | "vatNumber" | "email" | "phone" | "isActive";
  id: UUID;
  userId: UUID;
  name: string;
  addressLine1: string;
  addressLine2: string | null;
  city: string;
  postalCode: string;
  country: string;
  vatNumber: string | null;
  email: string | null;
  phone: string | null;
  isActive: boolean;
};



// Invoice Schema
export type InvoiceResourceSchema = {
  __type: "Resource";
  __primitiveFields: "id" | "userId" | "serialNumber" | "state" | "issueDate" | "dueDate" | "companyName" | "companyAddressLine1" | "companyAddressLine2" | "companyCity" | "companyPostalCode" | "companyCountry" | "companyVatNumber" | "companyEmail" | "companyPhone" | "customerName" | "customerAddressLine1" | "customerAddressLine2" | "customerCity" | "customerPostalCode" | "customerCountry" | "customerVatNumber" | "customerEmail" | "customerPhone" | "currency";
  id: UUID;
  userId: UUID;
  serialNumber: string | null;
  state: "draft" | "finalized" | "cancelled";
  issueDate: AshDate;
  dueDate: AshDate;
  companyName: string;
  companyAddressLine1: string;
  companyAddressLine2: string | null;
  companyCity: string;
  companyPostalCode: string;
  companyCountry: string;
  companyVatNumber: string | null;
  companyEmail: string | null;
  companyPhone: string | null;
  customerName: string;
  customerAddressLine1: string;
  customerAddressLine2: string | null;
  customerCity: string;
  customerPostalCode: string;
  customerCountry: string;
  customerVatNumber: string | null;
  customerEmail: string | null;
  customerPhone: string | null;
  currency: string;
};



// InvoiceLine Schema
export type InvoiceLineResourceSchema = {
  __type: "Resource";
  __primitiveFields: "id" | "invoiceId" | "lineNumber" | "description" | "quantity" | "unitPrice" | "taxRate";
  id: UUID;
  invoiceId: UUID;
  lineNumber: number;
  description: string;
  quantity: Decimal;
  unitPrice: Decimal;
  taxRate: Decimal;
};



// CreditNote Schema
export type CreditNoteResourceSchema = {
  __type: "Resource";
  __primitiveFields: "id" | "userId" | "serialNumber" | "state" | "issueDate" | "originalInvoiceId" | "creditReason" | "companyName" | "companyAddressLine1" | "companyAddressLine2" | "companyCity" | "companyPostalCode" | "companyCountry" | "companyVatNumber" | "companyEmail" | "companyPhone" | "customerName" | "customerAddressLine1" | "customerAddressLine2" | "customerCity" | "customerPostalCode" | "customerCountry" | "customerVatNumber" | "customerEmail" | "customerPhone" | "currency";
  id: UUID;
  userId: UUID;
  serialNumber: string | null;
  state: "draft" | "finalized" | "cancelled";
  issueDate: AshDate;
  originalInvoiceId: UUID | null;
  creditReason: string;
  companyName: string;
  companyAddressLine1: string;
  companyAddressLine2: string | null;
  companyCity: string;
  companyPostalCode: string;
  companyCountry: string;
  companyVatNumber: string | null;
  companyEmail: string | null;
  companyPhone: string | null;
  customerName: string;
  customerAddressLine1: string;
  customerAddressLine2: string | null;
  customerCity: string;
  customerPostalCode: string;
  customerCountry: string;
  customerVatNumber: string | null;
  customerEmail: string | null;
  customerPhone: string | null;
  currency: string;
};



// CreditNoteLine Schema
export type CreditNoteLineResourceSchema = {
  __type: "Resource";
  __primitiveFields: "id" | "creditNoteId" | "lineNumber" | "description" | "quantity" | "unitPrice" | "taxRate";
  id: UUID;
  creditNoteId: UUID;
  lineNumber: number;
  description: string;
  quantity: Decimal;
  unitPrice: Decimal;
  taxRate: Decimal;
};



// SequenceNumber Schema
export type SequenceNumberResourceSchema = {
  __type: "Resource";
  __primitiveFields: "id" | "userId" | "documentType" | "nextNumber";
  id: UUID;
  userId: UUID;
  documentType: "invoice" | "credit_note";
  nextNumber: number;
};



export type CompanyFilterInput = {
  and?: Array<CompanyFilterInput>;
  or?: Array<CompanyFilterInput>;
  not?: Array<CompanyFilterInput>;

  id?: {
    eq?: UUID;
    notEq?: UUID;
    in?: Array<UUID>;
  };

  userId?: {
    eq?: UUID;
    notEq?: UUID;
    in?: Array<UUID>;
  };

  name?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  addressLine1?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  addressLine2?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  city?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  postalCode?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  country?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  vatNumber?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  email?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  phone?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  isDefault?: {
    eq?: boolean;
    notEq?: boolean;
  };



};
export type CustomerFilterInput = {
  and?: Array<CustomerFilterInput>;
  or?: Array<CustomerFilterInput>;
  not?: Array<CustomerFilterInput>;

  id?: {
    eq?: UUID;
    notEq?: UUID;
    in?: Array<UUID>;
  };

  userId?: {
    eq?: UUID;
    notEq?: UUID;
    in?: Array<UUID>;
  };

  name?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  addressLine1?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  addressLine2?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  city?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  postalCode?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  country?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  vatNumber?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  email?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  phone?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  isActive?: {
    eq?: boolean;
    notEq?: boolean;
  };



};
export type InvoiceFilterInput = {
  and?: Array<InvoiceFilterInput>;
  or?: Array<InvoiceFilterInput>;
  not?: Array<InvoiceFilterInput>;

  id?: {
    eq?: UUID;
    notEq?: UUID;
    in?: Array<UUID>;
  };

  userId?: {
    eq?: UUID;
    notEq?: UUID;
    in?: Array<UUID>;
  };

  serialNumber?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  state?: {
    eq?: "draft" | "finalized" | "cancelled";
    notEq?: "draft" | "finalized" | "cancelled";
    in?: Array<"draft" | "finalized" | "cancelled">;
  };

  issueDate?: {
    eq?: AshDate;
    notEq?: AshDate;
    greaterThan?: AshDate;
    greaterThanOrEqual?: AshDate;
    lessThan?: AshDate;
    lessThanOrEqual?: AshDate;
    in?: Array<AshDate>;
  };

  dueDate?: {
    eq?: AshDate;
    notEq?: AshDate;
    greaterThan?: AshDate;
    greaterThanOrEqual?: AshDate;
    lessThan?: AshDate;
    lessThanOrEqual?: AshDate;
    in?: Array<AshDate>;
  };

  companyName?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  companyAddressLine1?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  companyAddressLine2?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  companyCity?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  companyPostalCode?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  companyCountry?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  companyVatNumber?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  companyEmail?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  companyPhone?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  customerName?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  customerAddressLine1?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  customerAddressLine2?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  customerCity?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  customerPostalCode?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  customerCountry?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  customerVatNumber?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  customerEmail?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  customerPhone?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  currency?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };



};
export type InvoiceLineFilterInput = {
  and?: Array<InvoiceLineFilterInput>;
  or?: Array<InvoiceLineFilterInput>;
  not?: Array<InvoiceLineFilterInput>;

  id?: {
    eq?: UUID;
    notEq?: UUID;
    in?: Array<UUID>;
  };

  invoiceId?: {
    eq?: UUID;
    notEq?: UUID;
    in?: Array<UUID>;
  };

  lineNumber?: {
    eq?: number;
    notEq?: number;
    greaterThan?: number;
    greaterThanOrEqual?: number;
    lessThan?: number;
    lessThanOrEqual?: number;
    in?: Array<number>;
  };

  description?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  quantity?: {
    eq?: Decimal;
    notEq?: Decimal;
    greaterThan?: Decimal;
    greaterThanOrEqual?: Decimal;
    lessThan?: Decimal;
    lessThanOrEqual?: Decimal;
    in?: Array<Decimal>;
  };

  unitPrice?: {
    eq?: Decimal;
    notEq?: Decimal;
    greaterThan?: Decimal;
    greaterThanOrEqual?: Decimal;
    lessThan?: Decimal;
    lessThanOrEqual?: Decimal;
    in?: Array<Decimal>;
  };

  taxRate?: {
    eq?: Decimal;
    notEq?: Decimal;
    greaterThan?: Decimal;
    greaterThanOrEqual?: Decimal;
    lessThan?: Decimal;
    lessThanOrEqual?: Decimal;
    in?: Array<Decimal>;
  };



};
export type CreditNoteFilterInput = {
  and?: Array<CreditNoteFilterInput>;
  or?: Array<CreditNoteFilterInput>;
  not?: Array<CreditNoteFilterInput>;

  id?: {
    eq?: UUID;
    notEq?: UUID;
    in?: Array<UUID>;
  };

  userId?: {
    eq?: UUID;
    notEq?: UUID;
    in?: Array<UUID>;
  };

  serialNumber?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  state?: {
    eq?: "draft" | "finalized" | "cancelled";
    notEq?: "draft" | "finalized" | "cancelled";
    in?: Array<"draft" | "finalized" | "cancelled">;
  };

  issueDate?: {
    eq?: AshDate;
    notEq?: AshDate;
    greaterThan?: AshDate;
    greaterThanOrEqual?: AshDate;
    lessThan?: AshDate;
    lessThanOrEqual?: AshDate;
    in?: Array<AshDate>;
  };

  originalInvoiceId?: {
    eq?: UUID;
    notEq?: UUID;
    in?: Array<UUID>;
  };

  creditReason?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  companyName?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  companyAddressLine1?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  companyAddressLine2?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  companyCity?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  companyPostalCode?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  companyCountry?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  companyVatNumber?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  companyEmail?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  companyPhone?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  customerName?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  customerAddressLine1?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  customerAddressLine2?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  customerCity?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  customerPostalCode?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  customerCountry?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  customerVatNumber?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  customerEmail?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  customerPhone?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  currency?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };



};
export type CreditNoteLineFilterInput = {
  and?: Array<CreditNoteLineFilterInput>;
  or?: Array<CreditNoteLineFilterInput>;
  not?: Array<CreditNoteLineFilterInput>;

  id?: {
    eq?: UUID;
    notEq?: UUID;
    in?: Array<UUID>;
  };

  creditNoteId?: {
    eq?: UUID;
    notEq?: UUID;
    in?: Array<UUID>;
  };

  lineNumber?: {
    eq?: number;
    notEq?: number;
    greaterThan?: number;
    greaterThanOrEqual?: number;
    lessThan?: number;
    lessThanOrEqual?: number;
    in?: Array<number>;
  };

  description?: {
    eq?: string;
    notEq?: string;
    in?: Array<string>;
  };

  quantity?: {
    eq?: Decimal;
    notEq?: Decimal;
    greaterThan?: Decimal;
    greaterThanOrEqual?: Decimal;
    lessThan?: Decimal;
    lessThanOrEqual?: Decimal;
    in?: Array<Decimal>;
  };

  unitPrice?: {
    eq?: Decimal;
    notEq?: Decimal;
    greaterThan?: Decimal;
    greaterThanOrEqual?: Decimal;
    lessThan?: Decimal;
    lessThanOrEqual?: Decimal;
    in?: Array<Decimal>;
  };

  taxRate?: {
    eq?: Decimal;
    notEq?: Decimal;
    greaterThan?: Decimal;
    greaterThanOrEqual?: Decimal;
    lessThan?: Decimal;
    lessThanOrEqual?: Decimal;
    in?: Array<Decimal>;
  };



};
export type SequenceNumberFilterInput = {
  and?: Array<SequenceNumberFilterInput>;
  or?: Array<SequenceNumberFilterInput>;
  not?: Array<SequenceNumberFilterInput>;

  id?: {
    eq?: UUID;
    notEq?: UUID;
    in?: Array<UUID>;
  };

  userId?: {
    eq?: UUID;
    notEq?: UUID;
    in?: Array<UUID>;
  };

  documentType?: {
    eq?: "invoice" | "credit_note";
    notEq?: "invoice" | "credit_note";
    in?: Array<"invoice" | "credit_note">;
  };

  nextNumber?: {
    eq?: number;
    notEq?: number;
    greaterThan?: number;
    greaterThanOrEqual?: number;
    lessThan?: number;
    lessThanOrEqual?: number;
    in?: Array<number>;
  };



};


// Utility Types

// Resource schema constraint
type TypedSchema = {
  __type: "Resource" | "TypedStruct" | "TypedMap" | "Union";
  __primitiveFields: string;
};

// Utility type to convert union to intersection
type UnionToIntersection<U> = (U extends any ? (k: U) => void : never) extends (
  k: infer I,
) => void
  ? I
  : never;

type HasComplexFields<T extends TypedSchema> = keyof Omit<
  T,
  "__primitiveFields" | "__type" | T["__primitiveFields"]
> extends never
  ? false
  : true;

type ComplexFieldKeys<T extends TypedSchema> = keyof Omit<
  T,
  "__primitiveFields" | "__type" | T["__primitiveFields"]
>;

type LeafFieldSelection<T extends TypedSchema> = T["__primitiveFields"];

type ComplexFieldSelection<T extends TypedSchema> = {
  [K in ComplexFieldKeys<T>]?: T[K] extends {
    __type: "Relationship";
    __resource: infer Resource;
  }
    ? NonNullable<Resource> extends TypedSchema
      ? UnifiedFieldSelection<NonNullable<Resource>>[]
      : never
    : T[K] extends {
          __type: "ComplexCalculation";
          __returnType: infer ReturnType;
        }
      ? T[K] extends { __args: infer Args }
        ? NonNullable<ReturnType> extends TypedSchema
          ? {
              args: Args;
              fields: UnifiedFieldSelection<NonNullable<ReturnType>>[];
            }
          : { args: Args }
        : NonNullable<ReturnType> extends TypedSchema
          ? { fields: UnifiedFieldSelection<NonNullable<ReturnType>>[] }
          : never
      : NonNullable<T[K]> extends TypedSchema
        ? UnifiedFieldSelection<NonNullable<T[K]>>[]
        : never;
};

// Main type: Use explicit base case detection to prevent infinite recursion
type UnifiedFieldSelection<T extends TypedSchema> =
  HasComplexFields<T> extends false
    ? LeafFieldSelection<T> // Base case: only primitives, no recursion
    : LeafFieldSelection<T> | ComplexFieldSelection<T>; // Recursive case

type InferFieldValue<
  T extends TypedSchema,
  Field,
> = Field extends T["__primitiveFields"]
  ? Field extends keyof T
    ? { [K in Field]: T[Field] }
    : never
  : Field extends Record<string, any>
    ? {
        [K in keyof Field]: K extends keyof T
          ? T[K] extends {
              __type: "Relationship";
              __resource: infer Resource;
            }
            ? NonNullable<Resource> extends TypedSchema
              ? T[K] extends { __array: true }
                ? Array<InferResult<NonNullable<Resource>, Field[K]>>
                : null extends Resource
                  ? InferResult<NonNullable<Resource>, Field[K]> | null
                  : InferResult<NonNullable<Resource>, Field[K]>
            : never
          : T[K] extends {
                __type: "ComplexCalculation";
                __returnType: infer ReturnType;
              }
            ? NonNullable<ReturnType> extends TypedSchema
              ? null extends ReturnType
                ? InferResult<NonNullable<ReturnType>, Field[K]["fields"]> | null
                : InferResult<NonNullable<ReturnType>, Field[K]["fields"]>
              : ReturnType
            : NonNullable<T[K]> extends TypedSchema
              ? null extends T[K]
                ? InferResult<NonNullable<T[K]>, Field[K]> | null
                : InferResult<NonNullable<T[K]>, Field[K]>
              : never
          : never;
      }
    : never;

type InferResult<
  T extends TypedSchema,
  SelectedFields extends UnifiedFieldSelection<T>[],
> = UnionToIntersection<
  {
    [K in keyof SelectedFields]: InferFieldValue<T, SelectedFields[K]>;
  }[number]
>;


// Helper Functions

/**
 * Gets the CSRF token from the page's meta tag
 * Returns null if no CSRF token is found
 */
export function getPhoenixCSRFToken(): string | null {
  return document
    ?.querySelector("meta[name='csrf-token']")
    ?.getAttribute("content") || null;
}

/**
 * Builds headers object with CSRF token for Phoenix applications
 * Returns headers object with X-CSRF-Token (if available)
 */
export function buildCSRFHeaders(): Record<string, string> {
  const headers: Record<string, string> = {};

  const csrfToken = getPhoenixCSRFToken();
  if (csrfToken) {
    headers["X-CSRF-Token"] = csrfToken;
  }

  return headers;
}





export type CreateCompanyInput = {
  name: string;
  addressLine1: string;
  addressLine2?: string | null;
  city: string;
  postalCode: string;
  country: string;
  vatNumber?: string | null;
  email?: string | null;
  phone?: string | null;
  isDefault?: boolean;
};


type InferCreateCompanyResult<
  Fields extends UnifiedFieldSelection<CompanyResourceSchema>[],
> = InferResult<CompanyResourceSchema, Fields>;


export type CreateCompanyResult<Fields extends UnifiedFieldSelection<CompanyResourceSchema>[]> = | { success: true; data: InferCreateCompanyResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function createCompany<Fields extends UnifiedFieldSelection<CompanyResourceSchema>[]>(
  config: {
  input: CreateCompanyInput;
  fields: Fields;
  headers?: Record<string, string>;
}
): Promise<CreateCompanyResult<Fields>> {
  const payload = {
    action: "create_company",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as CreateCompanyResult<Fields>;
}


export type ValidateCreateCompanyResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateCreateCompany(
  config: {
  input: CreateCompanyInput;
  headers?: Record<string, string>;
}
): Promise<ValidateCreateCompanyResult> {
  const payload = {
    action: "create_company",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateCreateCompanyResult;
}




export type GetCompanyInput = {};


type InferGetCompanyResult<
  Fields extends UnifiedFieldSelection<CompanyResourceSchema>[],
> = {
  results: Array<InferResult<CompanyResourceSchema, Fields>>;
  hasMore: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
} | {
  results: Array<InferResult<CompanyResourceSchema, Fields>>;
  hasMore: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previousPage: string;
  nextPage: string;
  count?: number | null;
  type: "keyset";
};


export type GetCompanyResult<Fields extends UnifiedFieldSelection<CompanyResourceSchema>[]> = | { success: true; data: InferGetCompanyResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function getCompany<Fields extends UnifiedFieldSelection<CompanyResourceSchema>[]>(
  config: {
  input: GetCompanyInput;
  fields: Fields;
  filter?: CompanyFilterInput;
  sort?: string;
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
}
): Promise<GetCompanyResult<Fields>> {
  const payload = {
    action: "get_company",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as GetCompanyResult<Fields>;
}


export type ValidateGetCompanyResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateGetCompany(
  config: {
  input: GetCompanyInput;
  headers?: Record<string, string>;
}
): Promise<ValidateGetCompanyResult> {
  const payload = {
    action: "get_company",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateGetCompanyResult;
}




export type UpdateCompanyInput = {
  name: string;
  addressLine1: string;
  addressLine2?: string | null;
  city: string;
  postalCode: string;
  country: string;
  vatNumber?: string | null;
  email?: string | null;
  phone?: string | null;
  isDefault?: boolean;
};


type InferUpdateCompanyResult<
  Fields extends UnifiedFieldSelection<CompanyResourceSchema>[],
> = InferResult<CompanyResourceSchema, Fields>;


export type UpdateCompanyResult<Fields extends UnifiedFieldSelection<CompanyResourceSchema>[]> = | { success: true; data: InferUpdateCompanyResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function updateCompany<Fields extends UnifiedFieldSelection<CompanyResourceSchema>[]>(
  config: {
  primaryKey: UUID;
  input: UpdateCompanyInput;
  fields: Fields;
  headers?: Record<string, string>;
}
): Promise<UpdateCompanyResult<Fields>> {
  const payload = {
    action: "update_company",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as UpdateCompanyResult<Fields>;
}


export type ValidateUpdateCompanyResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateUpdateCompany(
  config: {
  primaryKey: string;
  input: UpdateCompanyInput;
  headers?: Record<string, string>;
}
): Promise<ValidateUpdateCompanyResult> {
  const payload = {
    action: "update_company",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateUpdateCompanyResult;
}




export type DeleteCompanyInput = {};




export type DeleteCompanyResult = | { success: true; data: {} }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function deleteCompany(
  config: {
  primaryKey: UUID;
  input: DeleteCompanyInput;
  headers?: Record<string, string>;
}
): Promise<DeleteCompanyResult> {
  const payload = {
    action: "delete_company",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as DeleteCompanyResult;
}


export type ValidateDeleteCompanyResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateDeleteCompany(
  config: {
  primaryKey: string;
  input: DeleteCompanyInput;
  headers?: Record<string, string>;
}
): Promise<ValidateDeleteCompanyResult> {
  const payload = {
    action: "delete_company",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateDeleteCompanyResult;
}




export type ListCompaniesInput = {};


type InferListCompaniesResult<
  Fields extends UnifiedFieldSelection<CompanyResourceSchema>[],
> = Array<InferResult<CompanyResourceSchema, Fields>>;


export type ListCompaniesResult<Fields extends UnifiedFieldSelection<CompanyResourceSchema>[]> = | { success: true; data: InferListCompaniesResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function listCompanies<Fields extends UnifiedFieldSelection<CompanyResourceSchema>[]>(
  config: {
  input: ListCompaniesInput;
  fields: Fields;
  filter?: CompanyFilterInput;
  sort?: string;
  headers?: Record<string, string>;
}
): Promise<ListCompaniesResult<Fields>> {
  const payload = {
    action: "list_companies",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as ListCompaniesResult<Fields>;
}


export type ValidateListCompaniesResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateListCompanies(
  config: {
  input: ListCompaniesInput;
  headers?: Record<string, string>;
}
): Promise<ValidateListCompaniesResult> {
  const payload = {
    action: "list_companies",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateListCompaniesResult;
}




export type GetDefaultCompanyInput = {};


type InferGetDefaultCompanyResult<
  Fields extends UnifiedFieldSelection<CompanyResourceSchema>[],
> = Array<InferResult<CompanyResourceSchema, Fields>>;


export type GetDefaultCompanyResult<Fields extends UnifiedFieldSelection<CompanyResourceSchema>[]> = | { success: true; data: InferGetDefaultCompanyResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function getDefaultCompany<Fields extends UnifiedFieldSelection<CompanyResourceSchema>[]>(
  config: {
  input: GetDefaultCompanyInput;
  fields: Fields;
  filter?: CompanyFilterInput;
  sort?: string;
  headers?: Record<string, string>;
}
): Promise<GetDefaultCompanyResult<Fields>> {
  const payload = {
    action: "get_default_company",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as GetDefaultCompanyResult<Fields>;
}


export type ValidateGetDefaultCompanyResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateGetDefaultCompany(
  config: {
  input: GetDefaultCompanyInput;
  headers?: Record<string, string>;
}
): Promise<ValidateGetDefaultCompanyResult> {
  const payload = {
    action: "get_default_company",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateGetDefaultCompanyResult;
}




export type CreateCustomerInput = {
  name: string;
  addressLine1: string;
  addressLine2?: string | null;
  city: string;
  postalCode: string;
  country: string;
  vatNumber?: string | null;
  email?: string | null;
  phone?: string | null;
  isActive?: boolean;
};


type InferCreateCustomerResult<
  Fields extends UnifiedFieldSelection<CustomerResourceSchema>[],
> = InferResult<CustomerResourceSchema, Fields>;


export type CreateCustomerResult<Fields extends UnifiedFieldSelection<CustomerResourceSchema>[]> = | { success: true; data: InferCreateCustomerResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function createCustomer<Fields extends UnifiedFieldSelection<CustomerResourceSchema>[]>(
  config: {
  input: CreateCustomerInput;
  fields: Fields;
  headers?: Record<string, string>;
}
): Promise<CreateCustomerResult<Fields>> {
  const payload = {
    action: "create_customer",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as CreateCustomerResult<Fields>;
}


export type ValidateCreateCustomerResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateCreateCustomer(
  config: {
  input: CreateCustomerInput;
  headers?: Record<string, string>;
}
): Promise<ValidateCreateCustomerResult> {
  const payload = {
    action: "create_customer",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateCreateCustomerResult;
}




export type GetCustomerInput = {};


type InferGetCustomerResult<
  Fields extends UnifiedFieldSelection<CustomerResourceSchema>[],
> = {
  results: Array<InferResult<CustomerResourceSchema, Fields>>;
  hasMore: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
} | {
  results: Array<InferResult<CustomerResourceSchema, Fields>>;
  hasMore: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previousPage: string;
  nextPage: string;
  count?: number | null;
  type: "keyset";
};


export type GetCustomerResult<Fields extends UnifiedFieldSelection<CustomerResourceSchema>[]> = | { success: true; data: InferGetCustomerResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function getCustomer<Fields extends UnifiedFieldSelection<CustomerResourceSchema>[]>(
  config: {
  input: GetCustomerInput;
  fields: Fields;
  filter?: CustomerFilterInput;
  sort?: string;
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
}
): Promise<GetCustomerResult<Fields>> {
  const payload = {
    action: "get_customer",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as GetCustomerResult<Fields>;
}


export type ValidateGetCustomerResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateGetCustomer(
  config: {
  input: GetCustomerInput;
  headers?: Record<string, string>;
}
): Promise<ValidateGetCustomerResult> {
  const payload = {
    action: "get_customer",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateGetCustomerResult;
}




export type UpdateCustomerInput = {
  name: string;
  addressLine1: string;
  addressLine2?: string | null;
  city: string;
  postalCode: string;
  country: string;
  vatNumber?: string | null;
  email?: string | null;
  phone?: string | null;
  isActive?: boolean;
};


type InferUpdateCustomerResult<
  Fields extends UnifiedFieldSelection<CustomerResourceSchema>[],
> = InferResult<CustomerResourceSchema, Fields>;


export type UpdateCustomerResult<Fields extends UnifiedFieldSelection<CustomerResourceSchema>[]> = | { success: true; data: InferUpdateCustomerResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function updateCustomer<Fields extends UnifiedFieldSelection<CustomerResourceSchema>[]>(
  config: {
  primaryKey: UUID;
  input: UpdateCustomerInput;
  fields: Fields;
  headers?: Record<string, string>;
}
): Promise<UpdateCustomerResult<Fields>> {
  const payload = {
    action: "update_customer",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as UpdateCustomerResult<Fields>;
}


export type ValidateUpdateCustomerResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateUpdateCustomer(
  config: {
  primaryKey: string;
  input: UpdateCustomerInput;
  headers?: Record<string, string>;
}
): Promise<ValidateUpdateCustomerResult> {
  const payload = {
    action: "update_customer",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateUpdateCustomerResult;
}




export type DeleteCustomerInput = {};




export type DeleteCustomerResult = | { success: true; data: {} }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function deleteCustomer(
  config: {
  primaryKey: UUID;
  input: DeleteCustomerInput;
  headers?: Record<string, string>;
}
): Promise<DeleteCustomerResult> {
  const payload = {
    action: "delete_customer",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as DeleteCustomerResult;
}


export type ValidateDeleteCustomerResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateDeleteCustomer(
  config: {
  primaryKey: string;
  input: DeleteCustomerInput;
  headers?: Record<string, string>;
}
): Promise<ValidateDeleteCustomerResult> {
  const payload = {
    action: "delete_customer",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateDeleteCustomerResult;
}




export type ListActiveCustomersInput = {};


type InferListActiveCustomersResult<
  Fields extends UnifiedFieldSelection<CustomerResourceSchema>[],
> = Array<InferResult<CustomerResourceSchema, Fields>>;


export type ListActiveCustomersResult<Fields extends UnifiedFieldSelection<CustomerResourceSchema>[]> = | { success: true; data: InferListActiveCustomersResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function listActiveCustomers<Fields extends UnifiedFieldSelection<CustomerResourceSchema>[]>(
  config: {
  input: ListActiveCustomersInput;
  fields: Fields;
  filter?: CustomerFilterInput;
  sort?: string;
  headers?: Record<string, string>;
}
): Promise<ListActiveCustomersResult<Fields>> {
  const payload = {
    action: "list_active_customers",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as ListActiveCustomersResult<Fields>;
}


export type ValidateListActiveCustomersResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateListActiveCustomers(
  config: {
  input: ListActiveCustomersInput;
  headers?: Record<string, string>;
}
): Promise<ValidateListActiveCustomersResult> {
  const payload = {
    action: "list_active_customers",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateListActiveCustomersResult;
}




export type DeactivateCustomerInput = {};


type InferDeactivateCustomerResult<
  Fields extends UnifiedFieldSelection<CustomerResourceSchema>[],
> = InferResult<CustomerResourceSchema, Fields>;


export type DeactivateCustomerResult<Fields extends UnifiedFieldSelection<CustomerResourceSchema>[]> = | { success: true; data: InferDeactivateCustomerResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function deactivateCustomer<Fields extends UnifiedFieldSelection<CustomerResourceSchema>[]>(
  config: {
  primaryKey: UUID;
  input: DeactivateCustomerInput;
  fields: Fields;
  headers?: Record<string, string>;
}
): Promise<DeactivateCustomerResult<Fields>> {
  const payload = {
    action: "deactivate_customer",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as DeactivateCustomerResult<Fields>;
}


export type ValidateDeactivateCustomerResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateDeactivateCustomer(
  config: {
  primaryKey: string;
  input: DeactivateCustomerInput;
  headers?: Record<string, string>;
}
): Promise<ValidateDeactivateCustomerResult> {
  const payload = {
    action: "deactivate_customer",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateDeactivateCustomerResult;
}




export type ActivateCustomerInput = {};


type InferActivateCustomerResult<
  Fields extends UnifiedFieldSelection<CustomerResourceSchema>[],
> = InferResult<CustomerResourceSchema, Fields>;


export type ActivateCustomerResult<Fields extends UnifiedFieldSelection<CustomerResourceSchema>[]> = | { success: true; data: InferActivateCustomerResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function activateCustomer<Fields extends UnifiedFieldSelection<CustomerResourceSchema>[]>(
  config: {
  primaryKey: UUID;
  input: ActivateCustomerInput;
  fields: Fields;
  headers?: Record<string, string>;
}
): Promise<ActivateCustomerResult<Fields>> {
  const payload = {
    action: "activate_customer",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as ActivateCustomerResult<Fields>;
}


export type ValidateActivateCustomerResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateActivateCustomer(
  config: {
  primaryKey: string;
  input: ActivateCustomerInput;
  headers?: Record<string, string>;
}
): Promise<ValidateActivateCustomerResult> {
  const payload = {
    action: "activate_customer",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateActivateCustomerResult;
}




export type CreateInvoiceInput = {
  issueDate: AshDate;
  dueDate: AshDate;
  currency?: string;
  companyName: string;
  companyAddressLine1: string;
  companyAddressLine2?: string | null;
  companyCity: string;
  companyPostalCode: string;
  companyCountry: string;
  companyVatNumber?: string | null;
  companyEmail?: string | null;
  companyPhone?: string | null;
  customerName: string;
  customerAddressLine1: string;
  customerAddressLine2?: string | null;
  customerCity: string;
  customerPostalCode: string;
  customerCountry: string;
  customerVatNumber?: string | null;
  customerEmail?: string | null;
  customerPhone?: string | null;
};


type InferCreateInvoiceResult<
  Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[],
> = InferResult<InvoiceResourceSchema, Fields>;


export type CreateInvoiceResult<Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[]> = | { success: true; data: InferCreateInvoiceResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function createInvoice<Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[]>(
  config: {
  input: CreateInvoiceInput;
  fields: Fields;
  headers?: Record<string, string>;
}
): Promise<CreateInvoiceResult<Fields>> {
  const payload = {
    action: "create_invoice",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as CreateInvoiceResult<Fields>;
}


export type ValidateCreateInvoiceResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateCreateInvoice(
  config: {
  input: CreateInvoiceInput;
  headers?: Record<string, string>;
}
): Promise<ValidateCreateInvoiceResult> {
  const payload = {
    action: "create_invoice",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateCreateInvoiceResult;
}




export type GetInvoiceInput = {};


type InferGetInvoiceResult<
  Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[],
> = {
  results: Array<InferResult<InvoiceResourceSchema, Fields>>;
  hasMore: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
} | {
  results: Array<InferResult<InvoiceResourceSchema, Fields>>;
  hasMore: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previousPage: string;
  nextPage: string;
  count?: number | null;
  type: "keyset";
};


export type GetInvoiceResult<Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[]> = | { success: true; data: InferGetInvoiceResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function getInvoice<Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[]>(
  config: {
  input: GetInvoiceInput;
  fields: Fields;
  filter?: InvoiceFilterInput;
  sort?: string;
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
}
): Promise<GetInvoiceResult<Fields>> {
  const payload = {
    action: "get_invoice",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as GetInvoiceResult<Fields>;
}


export type ValidateGetInvoiceResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateGetInvoice(
  config: {
  input: GetInvoiceInput;
  headers?: Record<string, string>;
}
): Promise<ValidateGetInvoiceResult> {
  const payload = {
    action: "get_invoice",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateGetInvoiceResult;
}




export type UpdateInvoiceInput = {
  issueDate: AshDate;
  dueDate: AshDate;
  currency?: string;
  companyName: string;
  companyAddressLine1: string;
  companyAddressLine2?: string | null;
  companyCity: string;
  companyPostalCode: string;
  companyCountry: string;
  companyVatNumber?: string | null;
  companyEmail?: string | null;
  companyPhone?: string | null;
  customerName: string;
  customerAddressLine1: string;
  customerAddressLine2?: string | null;
  customerCity: string;
  customerPostalCode: string;
  customerCountry: string;
  customerVatNumber?: string | null;
  customerEmail?: string | null;
  customerPhone?: string | null;
};


type InferUpdateInvoiceResult<
  Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[],
> = InferResult<InvoiceResourceSchema, Fields>;


export type UpdateInvoiceResult<Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[]> = | { success: true; data: InferUpdateInvoiceResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function updateInvoice<Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[]>(
  config: {
  primaryKey: UUID;
  input: UpdateInvoiceInput;
  fields: Fields;
  headers?: Record<string, string>;
}
): Promise<UpdateInvoiceResult<Fields>> {
  const payload = {
    action: "update_invoice",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as UpdateInvoiceResult<Fields>;
}


export type ValidateUpdateInvoiceResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateUpdateInvoice(
  config: {
  primaryKey: string;
  input: UpdateInvoiceInput;
  headers?: Record<string, string>;
}
): Promise<ValidateUpdateInvoiceResult> {
  const payload = {
    action: "update_invoice",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateUpdateInvoiceResult;
}




export type DeleteInvoiceInput = {};




export type DeleteInvoiceResult = | { success: true; data: {} }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function deleteInvoice(
  config: {
  primaryKey: UUID;
  input: DeleteInvoiceInput;
  headers?: Record<string, string>;
}
): Promise<DeleteInvoiceResult> {
  const payload = {
    action: "delete_invoice",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as DeleteInvoiceResult;
}


export type ValidateDeleteInvoiceResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateDeleteInvoice(
  config: {
  primaryKey: string;
  input: DeleteInvoiceInput;
  headers?: Record<string, string>;
}
): Promise<ValidateDeleteInvoiceResult> {
  const payload = {
    action: "delete_invoice",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateDeleteInvoiceResult;
}




export type ListInvoicesInput = {};


type InferListInvoicesResult<
  Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[],
> = Array<InferResult<InvoiceResourceSchema, Fields>>;


export type ListInvoicesResult<Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[]> = | { success: true; data: InferListInvoicesResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function listInvoices<Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[]>(
  config: {
  input: ListInvoicesInput;
  fields: Fields;
  filter?: InvoiceFilterInput;
  sort?: string;
  headers?: Record<string, string>;
}
): Promise<ListInvoicesResult<Fields>> {
  const payload = {
    action: "list_invoices",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as ListInvoicesResult<Fields>;
}


export type ValidateListInvoicesResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateListInvoices(
  config: {
  input: ListInvoicesInput;
  headers?: Record<string, string>;
}
): Promise<ValidateListInvoicesResult> {
  const payload = {
    action: "list_invoices",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateListInvoicesResult;
}




export type ListInvoicesByStateInput = {
  state: string;
};


type InferListInvoicesByStateResult<
  Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[],
> = Array<InferResult<InvoiceResourceSchema, Fields>>;


export type ListInvoicesByStateResult<Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[]> = | { success: true; data: InferListInvoicesByStateResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function listInvoicesByState<Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[]>(
  config: {
  input: ListInvoicesByStateInput;
  fields: Fields;
  filter?: InvoiceFilterInput;
  sort?: string;
  headers?: Record<string, string>;
}
): Promise<ListInvoicesByStateResult<Fields>> {
  const payload = {
    action: "list_invoices_by_state",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as ListInvoicesByStateResult<Fields>;
}


export type ValidateListInvoicesByStateResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateListInvoicesByState(
  config: {
  input: ListInvoicesByStateInput;
  headers?: Record<string, string>;
}
): Promise<ValidateListInvoicesByStateResult> {
  const payload = {
    action: "list_invoices_by_state",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateListInvoicesByStateResult;
}




export type FinalizeInvoiceInput = {};


type InferFinalizeInvoiceResult<
  Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[],
> = InferResult<InvoiceResourceSchema, Fields>;


export type FinalizeInvoiceResult<Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[]> = | { success: true; data: InferFinalizeInvoiceResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function finalizeInvoice<Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[]>(
  config: {
  primaryKey: UUID;
  input: FinalizeInvoiceInput;
  fields: Fields;
  headers?: Record<string, string>;
}
): Promise<FinalizeInvoiceResult<Fields>> {
  const payload = {
    action: "finalize_invoice",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as FinalizeInvoiceResult<Fields>;
}


export type ValidateFinalizeInvoiceResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateFinalizeInvoice(
  config: {
  primaryKey: string;
  input: FinalizeInvoiceInput;
  headers?: Record<string, string>;
}
): Promise<ValidateFinalizeInvoiceResult> {
  const payload = {
    action: "finalize_invoice",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateFinalizeInvoiceResult;
}




export type CancelInvoiceInput = {};


type InferCancelInvoiceResult<
  Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[],
> = InferResult<InvoiceResourceSchema, Fields>;


export type CancelInvoiceResult<Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[]> = | { success: true; data: InferCancelInvoiceResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function cancelInvoice<Fields extends UnifiedFieldSelection<InvoiceResourceSchema>[]>(
  config: {
  primaryKey: UUID;
  input: CancelInvoiceInput;
  fields: Fields;
  headers?: Record<string, string>;
}
): Promise<CancelInvoiceResult<Fields>> {
  const payload = {
    action: "cancel_invoice",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as CancelInvoiceResult<Fields>;
}


export type ValidateCancelInvoiceResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateCancelInvoice(
  config: {
  primaryKey: string;
  input: CancelInvoiceInput;
  headers?: Record<string, string>;
}
): Promise<ValidateCancelInvoiceResult> {
  const payload = {
    action: "cancel_invoice",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateCancelInvoiceResult;
}




export type CreateInvoiceLineInput = {
  invoiceId: UUID;
  lineNumber: number;
  description: string;
  quantity?: Decimal;
  unitPrice: Decimal;
  taxRate?: Decimal;
};


type InferCreateInvoiceLineResult<
  Fields extends UnifiedFieldSelection<InvoiceLineResourceSchema>[],
> = InferResult<InvoiceLineResourceSchema, Fields>;


export type CreateInvoiceLineResult<Fields extends UnifiedFieldSelection<InvoiceLineResourceSchema>[]> = | { success: true; data: InferCreateInvoiceLineResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function createInvoiceLine<Fields extends UnifiedFieldSelection<InvoiceLineResourceSchema>[]>(
  config: {
  input: CreateInvoiceLineInput;
  fields: Fields;
  headers?: Record<string, string>;
}
): Promise<CreateInvoiceLineResult<Fields>> {
  const payload = {
    action: "create_invoice_line",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as CreateInvoiceLineResult<Fields>;
}


export type ValidateCreateInvoiceLineResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateCreateInvoiceLine(
  config: {
  input: CreateInvoiceLineInput;
  headers?: Record<string, string>;
}
): Promise<ValidateCreateInvoiceLineResult> {
  const payload = {
    action: "create_invoice_line",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateCreateInvoiceLineResult;
}




export type GetInvoiceLineInput = {};


type InferGetInvoiceLineResult<
  Fields extends UnifiedFieldSelection<InvoiceLineResourceSchema>[],
> = {
  results: Array<InferResult<InvoiceLineResourceSchema, Fields>>;
  hasMore: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
} | {
  results: Array<InferResult<InvoiceLineResourceSchema, Fields>>;
  hasMore: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previousPage: string;
  nextPage: string;
  count?: number | null;
  type: "keyset";
};


export type GetInvoiceLineResult<Fields extends UnifiedFieldSelection<InvoiceLineResourceSchema>[]> = | { success: true; data: InferGetInvoiceLineResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function getInvoiceLine<Fields extends UnifiedFieldSelection<InvoiceLineResourceSchema>[]>(
  config: {
  input: GetInvoiceLineInput;
  fields: Fields;
  filter?: InvoiceLineFilterInput;
  sort?: string;
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
}
): Promise<GetInvoiceLineResult<Fields>> {
  const payload = {
    action: "get_invoice_line",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as GetInvoiceLineResult<Fields>;
}


export type ValidateGetInvoiceLineResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateGetInvoiceLine(
  config: {
  input: GetInvoiceLineInput;
  headers?: Record<string, string>;
}
): Promise<ValidateGetInvoiceLineResult> {
  const payload = {
    action: "get_invoice_line",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateGetInvoiceLineResult;
}




export type UpdateInvoiceLineInput = {
  lineNumber: number;
  description: string;
  quantity?: Decimal;
  unitPrice: Decimal;
  taxRate?: Decimal;
};


type InferUpdateInvoiceLineResult<
  Fields extends UnifiedFieldSelection<InvoiceLineResourceSchema>[],
> = InferResult<InvoiceLineResourceSchema, Fields>;


export type UpdateInvoiceLineResult<Fields extends UnifiedFieldSelection<InvoiceLineResourceSchema>[]> = | { success: true; data: InferUpdateInvoiceLineResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function updateInvoiceLine<Fields extends UnifiedFieldSelection<InvoiceLineResourceSchema>[]>(
  config: {
  primaryKey: UUID;
  input: UpdateInvoiceLineInput;
  fields: Fields;
  headers?: Record<string, string>;
}
): Promise<UpdateInvoiceLineResult<Fields>> {
  const payload = {
    action: "update_invoice_line",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as UpdateInvoiceLineResult<Fields>;
}


export type ValidateUpdateInvoiceLineResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateUpdateInvoiceLine(
  config: {
  primaryKey: string;
  input: UpdateInvoiceLineInput;
  headers?: Record<string, string>;
}
): Promise<ValidateUpdateInvoiceLineResult> {
  const payload = {
    action: "update_invoice_line",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateUpdateInvoiceLineResult;
}




export type DeleteInvoiceLineInput = {};




export type DeleteInvoiceLineResult = | { success: true; data: {} }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function deleteInvoiceLine(
  config: {
  primaryKey: UUID;
  input: DeleteInvoiceLineInput;
  headers?: Record<string, string>;
}
): Promise<DeleteInvoiceLineResult> {
  const payload = {
    action: "delete_invoice_line",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as DeleteInvoiceLineResult;
}


export type ValidateDeleteInvoiceLineResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateDeleteInvoiceLine(
  config: {
  primaryKey: string;
  input: DeleteInvoiceLineInput;
  headers?: Record<string, string>;
}
): Promise<ValidateDeleteInvoiceLineResult> {
  const payload = {
    action: "delete_invoice_line",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateDeleteInvoiceLineResult;
}




export type ListInvoiceLinesInput = {
  invoiceId: UUID;
};


type InferListInvoiceLinesResult<
  Fields extends UnifiedFieldSelection<InvoiceLineResourceSchema>[],
> = Array<InferResult<InvoiceLineResourceSchema, Fields>>;


export type ListInvoiceLinesResult<Fields extends UnifiedFieldSelection<InvoiceLineResourceSchema>[]> = | { success: true; data: InferListInvoiceLinesResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function listInvoiceLines<Fields extends UnifiedFieldSelection<InvoiceLineResourceSchema>[]>(
  config: {
  input: ListInvoiceLinesInput;
  fields: Fields;
  filter?: InvoiceLineFilterInput;
  sort?: string;
  headers?: Record<string, string>;
}
): Promise<ListInvoiceLinesResult<Fields>> {
  const payload = {
    action: "list_invoice_lines",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as ListInvoiceLinesResult<Fields>;
}


export type ValidateListInvoiceLinesResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateListInvoiceLines(
  config: {
  input: ListInvoiceLinesInput;
  headers?: Record<string, string>;
}
): Promise<ValidateListInvoiceLinesResult> {
  const payload = {
    action: "list_invoice_lines",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateListInvoiceLinesResult;
}




export type GetInvoiceLineByNumberInput = {
  invoiceId: UUID;
  lineNumber: number;
};


type InferGetInvoiceLineByNumberResult<
  Fields extends UnifiedFieldSelection<InvoiceLineResourceSchema>[],
> = Array<InferResult<InvoiceLineResourceSchema, Fields>>;


export type GetInvoiceLineByNumberResult<Fields extends UnifiedFieldSelection<InvoiceLineResourceSchema>[]> = | { success: true; data: InferGetInvoiceLineByNumberResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function getInvoiceLineByNumber<Fields extends UnifiedFieldSelection<InvoiceLineResourceSchema>[]>(
  config: {
  input: GetInvoiceLineByNumberInput;
  fields: Fields;
  filter?: InvoiceLineFilterInput;
  sort?: string;
  headers?: Record<string, string>;
}
): Promise<GetInvoiceLineByNumberResult<Fields>> {
  const payload = {
    action: "get_invoice_line_by_number",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as GetInvoiceLineByNumberResult<Fields>;
}


export type ValidateGetInvoiceLineByNumberResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateGetInvoiceLineByNumber(
  config: {
  input: GetInvoiceLineByNumberInput;
  headers?: Record<string, string>;
}
): Promise<ValidateGetInvoiceLineByNumberResult> {
  const payload = {
    action: "get_invoice_line_by_number",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateGetInvoiceLineByNumberResult;
}




export type CreateCreditNoteInput = {
  issueDate: AshDate;
  originalInvoiceId?: UUID | null;
  creditReason: string;
  currency?: string;
  companyName: string;
  companyAddressLine1: string;
  companyAddressLine2?: string | null;
  companyCity: string;
  companyPostalCode: string;
  companyCountry: string;
  companyVatNumber?: string | null;
  companyEmail?: string | null;
  companyPhone?: string | null;
  customerName: string;
  customerAddressLine1: string;
  customerAddressLine2?: string | null;
  customerCity: string;
  customerPostalCode: string;
  customerCountry: string;
  customerVatNumber?: string | null;
  customerEmail?: string | null;
  customerPhone?: string | null;
};


type InferCreateCreditNoteResult<
  Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[],
> = InferResult<CreditNoteResourceSchema, Fields>;


export type CreateCreditNoteResult<Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[]> = | { success: true; data: InferCreateCreditNoteResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function createCreditNote<Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[]>(
  config: {
  input: CreateCreditNoteInput;
  fields: Fields;
  headers?: Record<string, string>;
}
): Promise<CreateCreditNoteResult<Fields>> {
  const payload = {
    action: "create_credit_note",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as CreateCreditNoteResult<Fields>;
}


export type ValidateCreateCreditNoteResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateCreateCreditNote(
  config: {
  input: CreateCreditNoteInput;
  headers?: Record<string, string>;
}
): Promise<ValidateCreateCreditNoteResult> {
  const payload = {
    action: "create_credit_note",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateCreateCreditNoteResult;
}




export type CreateCreditNoteFromInvoiceInput = {
  invoiceId: UUID;
  creditReason: string;
};


type InferCreateCreditNoteFromInvoiceResult<
  Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[],
> = InferResult<CreditNoteResourceSchema, Fields>;


export type CreateCreditNoteFromInvoiceResult<Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[]> = | { success: true; data: InferCreateCreditNoteFromInvoiceResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function createCreditNoteFromInvoice<Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[]>(
  config: {
  input: CreateCreditNoteFromInvoiceInput;
  fields: Fields;
  headers?: Record<string, string>;
}
): Promise<CreateCreditNoteFromInvoiceResult<Fields>> {
  const payload = {
    action: "create_credit_note_from_invoice",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as CreateCreditNoteFromInvoiceResult<Fields>;
}


export type ValidateCreateCreditNoteFromInvoiceResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateCreateCreditNoteFromInvoice(
  config: {
  input: CreateCreditNoteFromInvoiceInput;
  headers?: Record<string, string>;
}
): Promise<ValidateCreateCreditNoteFromInvoiceResult> {
  const payload = {
    action: "create_credit_note_from_invoice",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateCreateCreditNoteFromInvoiceResult;
}




export type GetCreditNoteInput = {};


type InferGetCreditNoteResult<
  Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[],
> = {
  results: Array<InferResult<CreditNoteResourceSchema, Fields>>;
  hasMore: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
} | {
  results: Array<InferResult<CreditNoteResourceSchema, Fields>>;
  hasMore: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previousPage: string;
  nextPage: string;
  count?: number | null;
  type: "keyset";
};


export type GetCreditNoteResult<Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[]> = | { success: true; data: InferGetCreditNoteResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function getCreditNote<Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[]>(
  config: {
  input: GetCreditNoteInput;
  fields: Fields;
  filter?: CreditNoteFilterInput;
  sort?: string;
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
}
): Promise<GetCreditNoteResult<Fields>> {
  const payload = {
    action: "get_credit_note",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as GetCreditNoteResult<Fields>;
}


export type ValidateGetCreditNoteResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateGetCreditNote(
  config: {
  input: GetCreditNoteInput;
  headers?: Record<string, string>;
}
): Promise<ValidateGetCreditNoteResult> {
  const payload = {
    action: "get_credit_note",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateGetCreditNoteResult;
}




export type UpdateCreditNoteInput = {
  issueDate: AshDate;
  originalInvoiceId?: UUID | null;
  creditReason: string;
  currency?: string;
  companyName: string;
  companyAddressLine1: string;
  companyAddressLine2?: string | null;
  companyCity: string;
  companyPostalCode: string;
  companyCountry: string;
  companyVatNumber?: string | null;
  companyEmail?: string | null;
  companyPhone?: string | null;
  customerName: string;
  customerAddressLine1: string;
  customerAddressLine2?: string | null;
  customerCity: string;
  customerPostalCode: string;
  customerCountry: string;
  customerVatNumber?: string | null;
  customerEmail?: string | null;
  customerPhone?: string | null;
};


type InferUpdateCreditNoteResult<
  Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[],
> = InferResult<CreditNoteResourceSchema, Fields>;


export type UpdateCreditNoteResult<Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[]> = | { success: true; data: InferUpdateCreditNoteResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function updateCreditNote<Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[]>(
  config: {
  primaryKey: UUID;
  input: UpdateCreditNoteInput;
  fields: Fields;
  headers?: Record<string, string>;
}
): Promise<UpdateCreditNoteResult<Fields>> {
  const payload = {
    action: "update_credit_note",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as UpdateCreditNoteResult<Fields>;
}


export type ValidateUpdateCreditNoteResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateUpdateCreditNote(
  config: {
  primaryKey: string;
  input: UpdateCreditNoteInput;
  headers?: Record<string, string>;
}
): Promise<ValidateUpdateCreditNoteResult> {
  const payload = {
    action: "update_credit_note",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateUpdateCreditNoteResult;
}




export type DeleteCreditNoteInput = {};




export type DeleteCreditNoteResult = | { success: true; data: {} }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function deleteCreditNote(
  config: {
  primaryKey: UUID;
  input: DeleteCreditNoteInput;
  headers?: Record<string, string>;
}
): Promise<DeleteCreditNoteResult> {
  const payload = {
    action: "delete_credit_note",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as DeleteCreditNoteResult;
}


export type ValidateDeleteCreditNoteResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateDeleteCreditNote(
  config: {
  primaryKey: string;
  input: DeleteCreditNoteInput;
  headers?: Record<string, string>;
}
): Promise<ValidateDeleteCreditNoteResult> {
  const payload = {
    action: "delete_credit_note",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateDeleteCreditNoteResult;
}




export type ListCreditNotesInput = {};


type InferListCreditNotesResult<
  Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[],
> = Array<InferResult<CreditNoteResourceSchema, Fields>>;


export type ListCreditNotesResult<Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[]> = | { success: true; data: InferListCreditNotesResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function listCreditNotes<Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[]>(
  config: {
  input: ListCreditNotesInput;
  fields: Fields;
  filter?: CreditNoteFilterInput;
  sort?: string;
  headers?: Record<string, string>;
}
): Promise<ListCreditNotesResult<Fields>> {
  const payload = {
    action: "list_credit_notes",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as ListCreditNotesResult<Fields>;
}


export type ValidateListCreditNotesResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateListCreditNotes(
  config: {
  input: ListCreditNotesInput;
  headers?: Record<string, string>;
}
): Promise<ValidateListCreditNotesResult> {
  const payload = {
    action: "list_credit_notes",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateListCreditNotesResult;
}




export type ListCreditNotesByStateInput = {
  state: string;
};


type InferListCreditNotesByStateResult<
  Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[],
> = Array<InferResult<CreditNoteResourceSchema, Fields>>;


export type ListCreditNotesByStateResult<Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[]> = | { success: true; data: InferListCreditNotesByStateResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function listCreditNotesByState<Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[]>(
  config: {
  input: ListCreditNotesByStateInput;
  fields: Fields;
  filter?: CreditNoteFilterInput;
  sort?: string;
  headers?: Record<string, string>;
}
): Promise<ListCreditNotesByStateResult<Fields>> {
  const payload = {
    action: "list_credit_notes_by_state",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as ListCreditNotesByStateResult<Fields>;
}


export type ValidateListCreditNotesByStateResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateListCreditNotesByState(
  config: {
  input: ListCreditNotesByStateInput;
  headers?: Record<string, string>;
}
): Promise<ValidateListCreditNotesByStateResult> {
  const payload = {
    action: "list_credit_notes_by_state",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateListCreditNotesByStateResult;
}




export type ListCreditNotesForInvoiceInput = {
  invoiceId: UUID;
};


type InferListCreditNotesForInvoiceResult<
  Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[],
> = Array<InferResult<CreditNoteResourceSchema, Fields>>;


export type ListCreditNotesForInvoiceResult<Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[]> = | { success: true; data: InferListCreditNotesForInvoiceResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function listCreditNotesForInvoice<Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[]>(
  config: {
  input: ListCreditNotesForInvoiceInput;
  fields: Fields;
  filter?: CreditNoteFilterInput;
  sort?: string;
  headers?: Record<string, string>;
}
): Promise<ListCreditNotesForInvoiceResult<Fields>> {
  const payload = {
    action: "list_credit_notes_for_invoice",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as ListCreditNotesForInvoiceResult<Fields>;
}


export type ValidateListCreditNotesForInvoiceResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateListCreditNotesForInvoice(
  config: {
  input: ListCreditNotesForInvoiceInput;
  headers?: Record<string, string>;
}
): Promise<ValidateListCreditNotesForInvoiceResult> {
  const payload = {
    action: "list_credit_notes_for_invoice",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateListCreditNotesForInvoiceResult;
}




export type FinalizeCreditNoteInput = {};


type InferFinalizeCreditNoteResult<
  Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[],
> = InferResult<CreditNoteResourceSchema, Fields>;


export type FinalizeCreditNoteResult<Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[]> = | { success: true; data: InferFinalizeCreditNoteResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function finalizeCreditNote<Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[]>(
  config: {
  primaryKey: UUID;
  input: FinalizeCreditNoteInput;
  fields: Fields;
  headers?: Record<string, string>;
}
): Promise<FinalizeCreditNoteResult<Fields>> {
  const payload = {
    action: "finalize_credit_note",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as FinalizeCreditNoteResult<Fields>;
}


export type ValidateFinalizeCreditNoteResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateFinalizeCreditNote(
  config: {
  primaryKey: string;
  input: FinalizeCreditNoteInput;
  headers?: Record<string, string>;
}
): Promise<ValidateFinalizeCreditNoteResult> {
  const payload = {
    action: "finalize_credit_note",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateFinalizeCreditNoteResult;
}




export type CancelCreditNoteInput = {};


type InferCancelCreditNoteResult<
  Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[],
> = InferResult<CreditNoteResourceSchema, Fields>;


export type CancelCreditNoteResult<Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[]> = | { success: true; data: InferCancelCreditNoteResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function cancelCreditNote<Fields extends UnifiedFieldSelection<CreditNoteResourceSchema>[]>(
  config: {
  primaryKey: UUID;
  input: CancelCreditNoteInput;
  fields: Fields;
  headers?: Record<string, string>;
}
): Promise<CancelCreditNoteResult<Fields>> {
  const payload = {
    action: "cancel_credit_note",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as CancelCreditNoteResult<Fields>;
}


export type ValidateCancelCreditNoteResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateCancelCreditNote(
  config: {
  primaryKey: string;
  input: CancelCreditNoteInput;
  headers?: Record<string, string>;
}
): Promise<ValidateCancelCreditNoteResult> {
  const payload = {
    action: "cancel_credit_note",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateCancelCreditNoteResult;
}




export type CreateCreditNoteLineInput = {
  creditNoteId: UUID;
  lineNumber: number;
  description: string;
  quantity?: Decimal;
  unitPrice: Decimal;
  taxRate?: Decimal;
};


type InferCreateCreditNoteLineResult<
  Fields extends UnifiedFieldSelection<CreditNoteLineResourceSchema>[],
> = InferResult<CreditNoteLineResourceSchema, Fields>;


export type CreateCreditNoteLineResult<Fields extends UnifiedFieldSelection<CreditNoteLineResourceSchema>[]> = | { success: true; data: InferCreateCreditNoteLineResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function createCreditNoteLine<Fields extends UnifiedFieldSelection<CreditNoteLineResourceSchema>[]>(
  config: {
  input: CreateCreditNoteLineInput;
  fields: Fields;
  headers?: Record<string, string>;
}
): Promise<CreateCreditNoteLineResult<Fields>> {
  const payload = {
    action: "create_credit_note_line",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as CreateCreditNoteLineResult<Fields>;
}


export type ValidateCreateCreditNoteLineResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateCreateCreditNoteLine(
  config: {
  input: CreateCreditNoteLineInput;
  headers?: Record<string, string>;
}
): Promise<ValidateCreateCreditNoteLineResult> {
  const payload = {
    action: "create_credit_note_line",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateCreateCreditNoteLineResult;
}




export type GetCreditNoteLineInput = {};


type InferGetCreditNoteLineResult<
  Fields extends UnifiedFieldSelection<CreditNoteLineResourceSchema>[],
> = {
  results: Array<InferResult<CreditNoteLineResourceSchema, Fields>>;
  hasMore: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
} | {
  results: Array<InferResult<CreditNoteLineResourceSchema, Fields>>;
  hasMore: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previousPage: string;
  nextPage: string;
  count?: number | null;
  type: "keyset";
};


export type GetCreditNoteLineResult<Fields extends UnifiedFieldSelection<CreditNoteLineResourceSchema>[]> = | { success: true; data: InferGetCreditNoteLineResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function getCreditNoteLine<Fields extends UnifiedFieldSelection<CreditNoteLineResourceSchema>[]>(
  config: {
  input: GetCreditNoteLineInput;
  fields: Fields;
  filter?: CreditNoteLineFilterInput;
  sort?: string;
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
}
): Promise<GetCreditNoteLineResult<Fields>> {
  const payload = {
    action: "get_credit_note_line",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as GetCreditNoteLineResult<Fields>;
}


export type ValidateGetCreditNoteLineResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateGetCreditNoteLine(
  config: {
  input: GetCreditNoteLineInput;
  headers?: Record<string, string>;
}
): Promise<ValidateGetCreditNoteLineResult> {
  const payload = {
    action: "get_credit_note_line",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateGetCreditNoteLineResult;
}




export type UpdateCreditNoteLineInput = {
  lineNumber: number;
  description: string;
  quantity?: Decimal;
  unitPrice: Decimal;
  taxRate?: Decimal;
};


type InferUpdateCreditNoteLineResult<
  Fields extends UnifiedFieldSelection<CreditNoteLineResourceSchema>[],
> = InferResult<CreditNoteLineResourceSchema, Fields>;


export type UpdateCreditNoteLineResult<Fields extends UnifiedFieldSelection<CreditNoteLineResourceSchema>[]> = | { success: true; data: InferUpdateCreditNoteLineResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function updateCreditNoteLine<Fields extends UnifiedFieldSelection<CreditNoteLineResourceSchema>[]>(
  config: {
  primaryKey: UUID;
  input: UpdateCreditNoteLineInput;
  fields: Fields;
  headers?: Record<string, string>;
}
): Promise<UpdateCreditNoteLineResult<Fields>> {
  const payload = {
    action: "update_credit_note_line",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as UpdateCreditNoteLineResult<Fields>;
}


export type ValidateUpdateCreditNoteLineResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateUpdateCreditNoteLine(
  config: {
  primaryKey: string;
  input: UpdateCreditNoteLineInput;
  headers?: Record<string, string>;
}
): Promise<ValidateUpdateCreditNoteLineResult> {
  const payload = {
    action: "update_credit_note_line",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateUpdateCreditNoteLineResult;
}




export type DeleteCreditNoteLineInput = {};




export type DeleteCreditNoteLineResult = | { success: true; data: {} }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function deleteCreditNoteLine(
  config: {
  primaryKey: UUID;
  input: DeleteCreditNoteLineInput;
  headers?: Record<string, string>;
}
): Promise<DeleteCreditNoteLineResult> {
  const payload = {
    action: "delete_credit_note_line",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as DeleteCreditNoteLineResult;
}


export type ValidateDeleteCreditNoteLineResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateDeleteCreditNoteLine(
  config: {
  primaryKey: string;
  input: DeleteCreditNoteLineInput;
  headers?: Record<string, string>;
}
): Promise<ValidateDeleteCreditNoteLineResult> {
  const payload = {
    action: "delete_credit_note_line",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateDeleteCreditNoteLineResult;
}




export type ListCreditNoteLinesInput = {
  creditNoteId: UUID;
};


type InferListCreditNoteLinesResult<
  Fields extends UnifiedFieldSelection<CreditNoteLineResourceSchema>[],
> = Array<InferResult<CreditNoteLineResourceSchema, Fields>>;


export type ListCreditNoteLinesResult<Fields extends UnifiedFieldSelection<CreditNoteLineResourceSchema>[]> = | { success: true; data: InferListCreditNoteLinesResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function listCreditNoteLines<Fields extends UnifiedFieldSelection<CreditNoteLineResourceSchema>[]>(
  config: {
  input: ListCreditNoteLinesInput;
  fields: Fields;
  filter?: CreditNoteLineFilterInput;
  sort?: string;
  headers?: Record<string, string>;
}
): Promise<ListCreditNoteLinesResult<Fields>> {
  const payload = {
    action: "list_credit_note_lines",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as ListCreditNoteLinesResult<Fields>;
}


export type ValidateListCreditNoteLinesResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateListCreditNoteLines(
  config: {
  input: ListCreditNoteLinesInput;
  headers?: Record<string, string>;
}
): Promise<ValidateListCreditNoteLinesResult> {
  const payload = {
    action: "list_credit_note_lines",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateListCreditNoteLinesResult;
}




export type GetCreditNoteLineByNumberInput = {
  creditNoteId: UUID;
  lineNumber: number;
};


type InferGetCreditNoteLineByNumberResult<
  Fields extends UnifiedFieldSelection<CreditNoteLineResourceSchema>[],
> = Array<InferResult<CreditNoteLineResourceSchema, Fields>>;


export type GetCreditNoteLineByNumberResult<Fields extends UnifiedFieldSelection<CreditNoteLineResourceSchema>[]> = | { success: true; data: InferGetCreditNoteLineByNumberResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function getCreditNoteLineByNumber<Fields extends UnifiedFieldSelection<CreditNoteLineResourceSchema>[]>(
  config: {
  input: GetCreditNoteLineByNumberInput;
  fields: Fields;
  filter?: CreditNoteLineFilterInput;
  sort?: string;
  headers?: Record<string, string>;
}
): Promise<GetCreditNoteLineByNumberResult<Fields>> {
  const payload = {
    action: "get_credit_note_line_by_number",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as GetCreditNoteLineByNumberResult<Fields>;
}


export type ValidateGetCreditNoteLineByNumberResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateGetCreditNoteLineByNumber(
  config: {
  input: GetCreditNoteLineByNumberInput;
  headers?: Record<string, string>;
}
): Promise<ValidateGetCreditNoteLineByNumberResult> {
  const payload = {
    action: "get_credit_note_line_by_number",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateGetCreditNoteLineByNumberResult;
}




export type GetSequenceNumberInput = {};


type InferGetSequenceNumberResult<
  Fields extends UnifiedFieldSelection<SequenceNumberResourceSchema>[],
> = {
  results: Array<InferResult<SequenceNumberResourceSchema, Fields>>;
  hasMore: boolean;
  limit: number;
  offset: number;
  count?: number | null;
  type: "offset";
} | {
  results: Array<InferResult<SequenceNumberResourceSchema, Fields>>;
  hasMore: boolean;
  limit: number;
  after: string | null;
  before: string | null;
  previousPage: string;
  nextPage: string;
  count?: number | null;
  type: "keyset";
};


export type GetSequenceNumberResult<Fields extends UnifiedFieldSelection<SequenceNumberResourceSchema>[]> = | { success: true; data: InferGetSequenceNumberResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function getSequenceNumber<Fields extends UnifiedFieldSelection<SequenceNumberResourceSchema>[]>(
  config: {
  input: GetSequenceNumberInput;
  fields: Fields;
  filter?: SequenceNumberFilterInput;
  sort?: string;
  page?: (
    {
      limit?: number;
      offset?: number;
      count?: boolean;
    } | {
      limit?: number;
      after?: string;
      before?: string;
    }
  );
  headers?: Record<string, string>;
}
): Promise<GetSequenceNumberResult<Fields>> {
  const payload = {
    action: "get_sequence_number",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as GetSequenceNumberResult<Fields>;
}


export type ValidateGetSequenceNumberResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateGetSequenceNumber(
  config: {
  input: GetSequenceNumberInput;
  headers?: Record<string, string>;
}
): Promise<ValidateGetSequenceNumberResult> {
  const payload = {
    action: "get_sequence_number",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateGetSequenceNumberResult;
}




export type GetSequenceForUserAndTypeInput = {
  userId: UUID;
  documentType: string;
};


type InferGetSequenceForUserAndTypeResult<
  Fields extends UnifiedFieldSelection<SequenceNumberResourceSchema>[],
> = Array<InferResult<SequenceNumberResourceSchema, Fields>>;


export type GetSequenceForUserAndTypeResult<Fields extends UnifiedFieldSelection<SequenceNumberResourceSchema>[]> = | { success: true; data: InferGetSequenceForUserAndTypeResult<Fields> }
| {
    success: false;
    errors: Array<{
      type: string;
      message: string;
      fieldPath?: string;
      details: Record<string, string>;
    }>;
  }
;

export async function getSequenceForUserAndType<Fields extends UnifiedFieldSelection<SequenceNumberResourceSchema>[]>(
  config: {
  input: GetSequenceForUserAndTypeInput;
  fields: Fields;
  filter?: SequenceNumberFilterInput;
  sort?: string;
  headers?: Record<string, string>;
}
): Promise<GetSequenceForUserAndTypeResult<Fields>> {
  const payload = {
    action: "get_sequence_for_user_and_type",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/run", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText, details: {} }],
    };
  }

  const result = await response.json();
  return result as GetSequenceForUserAndTypeResult<Fields>;
}


export type ValidateGetSequenceForUserAndTypeResult =
  | { success: true }
  | {
      success: false;
      errors: Array<{
        type: string;
        message: string;
        field?: string;
        fieldPath?: string;
        details?: Record<string, any>;
      }>;
    };


export async function validateGetSequenceForUserAndType(
  config: {
  input: GetSequenceForUserAndTypeInput;
  headers?: Record<string, string>;
}
): Promise<ValidateGetSequenceForUserAndTypeResult> {
  const payload = {
    action: "get_sequence_for_user_and_type",
    ...config
  };

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...config.headers,
  };

  const response = await fetch("/rpc/validate", {
    method: "POST",
    headers,
    body: JSON.stringify(payload),
  });

  if (!response.ok) {
    return {
      success: false,
      errors: [{ type: "network", message: response.statusText }],
    };
  }

  const result = await response.json();
  return result as ValidateGetSequenceForUserAndTypeResult;
}




